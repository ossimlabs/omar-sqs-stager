pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-sqs-stager-app'

project(':omar-sqs-stager-app').projectDir = "${rootProject.projectDir}/apps/omar-sqs-stager-app" as File
